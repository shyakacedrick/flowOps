# SPA fallback for any static host that reads `_redirects` (Netlify,
# Cloudflare Pages). React Router owns every path that isn't a built
# asset; without this rule, hard-refreshing /admin/queues returns 404.
/*    /index.html   200
